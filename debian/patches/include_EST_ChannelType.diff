This patch is part of a series of patches for speech-tools for
Debian GNU/Linux, which primarily fix build errors with GCC 4.3 and newer.
Index: speech-tools/include/EST_ChannelType.h
===================================================================
--- speech-tools.orig/include/EST_ChannelType.h	2009-03-17 09:01:00.000000000 -0500
+++ speech-tools/include/EST_ChannelType.h	2009-03-17 09:01:30.000000000 -0500
@@ -135,7 +135,7 @@
   * D  = Number of levels of differentiation.
   * SE = Start=0 end=1
   */
-#define EST_CoefChannelId(CT,D,SE) ( (CT)<<3 | (D)<<1 & 6 | (SE)&1 )
+#define EST_CoefChannelId(CT,D,SE) ( ((CT)<<3) | ((D)<<1 & 6) | ((SE)&1) )
 
 /** Build a number representing a channel type for a single value which can
   * N = count starting from 0
