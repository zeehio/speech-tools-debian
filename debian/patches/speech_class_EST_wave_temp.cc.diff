This patch is part of a series of patches for speech-tools for
Debian GNU/Linux, which primarily fix build errors with GCC 4.3 and newer.

Index: b/speech_class/EST_wave_temp.cc
===================================================================
--- a/speech_class/EST_wave_temp.cc
+++ b/speech_class/EST_wave_temp.cc
@@ -110,7 +110,7 @@
     for (i = 0; i < size; ++i)
     {
       // cout << i << " " << a.a(i, channel) << " " << b.a(i, channel) << endl;
-      sum += fabs(float(a.a(i, channel) - b.a(i, channel)));
+      sum += abs(float(a.a(i, channel) - b.a(i, channel)));
     }
     return sum / size;
 }
